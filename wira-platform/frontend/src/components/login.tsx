import React from 'react';

export default function Login(){
    return (
        <div>
            {/* Componente de login — adicione campos/formulário aqui */}
            Login
        </div>
    );
}